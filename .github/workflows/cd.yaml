name: Build and Deploy proxy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: dovakiin0/proxy-m3u8:latest, dovakiin0/proxy-m3u8:${{ github.sha }}

      - name: Update infra
        env:
          APP_TOKEN: ${{ secrets.APP_TOKEN }}
          INFRA_REPO: ${{ secrets.INFRA_REPO }}
          IMAGE: dovakiin0/proxy-m3u8
          TAG: ${{ github.sha }}
        run: |
          set -e

          git config --global user.email "bot@kitsunee.online"
          git config --global user.name "kitsunee-bot"

          rm -rf infra
          git clone --depth 1 "https://x-access-token:${APP_TOKEN}@github.com/${INFRA_REPO}.git" infra
          cd infra

          sed -i "s|${IMAGE}:[a-zA-Z0-9._-]*|${IMAGE}:${TAG}|g" docker-compose.yml

          git add .
          git commit -m "chore(deploy): bump proxy-m3u8 to ${TAG}" || exit 0
          git push
